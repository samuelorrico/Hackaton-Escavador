# Git LFS — necessário para versionar os artefatos pré-treinados no
# repositório do Hugging Face Space (features_cache.parquet ~137 MB + *.joblib).
*.parquet filter=lfs diff=lfs merge=lfs -text
*.joblib filter=lfs diff=lfs merge=lfs -text
